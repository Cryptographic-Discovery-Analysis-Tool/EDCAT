"""Loaders for cited reference data under the repository's `data/` directory.

Nothing in this package may contain a value typed from memory. Every value it
returns comes from a file in `data/` that carries its own citation
(CLAUDE.md anti-hallucination rules).
"""
